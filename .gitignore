# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Claude
.claude/

# Server tokens (sensitive)
server/.tokens.json

# Environment — Porsche credentials, DB password, Google API key
.env
.env.*
!.env.example

# Local Postgres backups (host-level backup should cover this directory)
backups/

# Compiled server output
dist-server

# Probe payloads — raw trip history (dates + distances). This repo is PUBLIC;
# a dated record of when and how far the car was driven does not belong in it.
# Kept locally so probes taken days apart can be compared (#15).
docs/probes/
